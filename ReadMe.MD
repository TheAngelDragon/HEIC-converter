# HEIC Converter

Converts .HEIC and .HEIF files from a selected folder into a JPEG.

## Build

Use the command:

```
python -m PyInstaller --onefile --noconsole imageconverter.py
```

## Use
Just run the executable and select te folder where all the HEIC files are.